import { TokensData } from '../types/tokens-data.type'

export const tokensDataMock: TokensData = {
  accessToken: 'accessToken',
  accessTokenExpires: new Date(),
  refreshToken: 'refreshToken',
  refreshTokenExpires: new Date()
}
